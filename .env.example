# Public base URL used for canonical links, Open Graph and the sitemap.
NEXT_PUBLIC_SITE_URL=https://conroy.global

# Optional: base URL for the service layer (contact form / newsletter).
# Defaults to "/api" (the bundled Next.js route handlers) when unset.
NEXT_PUBLIC_API_BASE_URL=/api
